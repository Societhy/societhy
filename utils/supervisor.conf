[supervisord]
nodaemon=true

[program:mkdir]
command=mkdir "/societhy/.parity"

[program:mkdir2]
command=mkdir "/societhy/.parity/keys"

[program:git]
command=cd /parity && git pull

[program:parity]
command=parity --chain utils/genesis.json --rpcaddr 127.0.0.1 --jsonrpc-hosts all --jsonrpc-apis web3,eth,net,parity,traces,rpc,personal --author "a7411a06253a15eb8625183fc1edf0547601a754"

[program:ethminer]
command=ethminer -t 1